const add = (first, second) => {
    return first + second
}

export default add;

const subtract = (first, second) => {
    return first - second
}

export {subtract}

export const multiply = (first, second) => {
    return first * second
}
